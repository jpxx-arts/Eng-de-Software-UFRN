### Executar o projeto do backend

- O backend está em `/tomaladaka`.
- É necessário Java 21.
- Para executar a aplicação:
```
./mvnw spring-boot:run
```
- Para testar a API:
```sh
chmod +x test.sh

./test.sh
```